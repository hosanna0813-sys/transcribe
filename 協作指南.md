# 協作指南(給站長:怎麼跟 Claude 一起改這個專案最有效率)

這份是寫給**你**看的。技術細節都在 `CLAUDE.md`(Claude 每次開新對話會自動讀,
不用你貼給它),所以你只要照這份的習慣做,開發就會又快又不出錯。

---

## 1. 開新對話怎麼開場

直接說**目標**就好,一次一個,例如:
- 「幫我把儲值頁加上 LINE Pay」
- 「YouTube 下載又出現 ×× 錯誤」(貼完整錯誤訊息)

不用交代專案背景、不用貼舊對話——Claude 會自動讀 `CLAUDE.md` 接手。
一個對話專心做一件事,做完驗收再開下一件,比一次塞三個需求快。

## 2. 合併紀律(最重要的一條)

> **等 Claude 說「可以合併了」再按 Merge。**

之前發生過:PR 一開出來你就按了合併,但 Claude 後續還在往同一個 PR 補推改動,
結果那些改動**沒有上線**,過了一週才發現。現在 Claude 會一批改動開一個 PR、
開完明確跟你說「可以合併」;你只要不搶快,就不會再發生。

按合併的步驟:PR 頁面等 **CI 綠色勾勾** → **Merge pull request** → **Confirm**。

## 3. 合併之後,常見三種待辦(Claude 會在 PR 裡點名)

| 待辦 | 去哪裡做 |
|---|---|
| 跑資料庫 migration | Supabase → SQL Editor → New query → 貼上指定的 `v2/schema_phaseN.sql` 全文 → Run |
| 設定環境變數 | Render → 你的服務 → Environment → 填指定的 key → 存檔(會自動重新部署) |
| 重建家用中繼 | 家用電腦 `server\home` 資料夾開終端機 → `docker compose up -d --build` |

PR 說明的「部署後(營運者)」段落會寫清楚這次需要哪幾項;沒寫的就不用做。

## 4. 回報問題的黃金格式

三個要素,缺一不可:
1. **在哪一頁**(首頁 / 計費版 / 進階版)
2. **做了什麼**(例:貼了某支 YouTube 網址、上傳了 2 小時的 mp3)
3. **完整錯誤訊息**(整段複製貼上,或截圖;不要只說「壞了」)

有這三項,Claude 通常一輪就能定位;缺了就要來回猜,慢很多。

## 5. 驗收習慣

- 合併後 Render 部署約需 **2–3 分鐘**才生效。
- 網頁改版要用 **Ctrl+Shift+R**(Mac:Cmd+Shift+R)強制刷新,否則看到的是瀏覽器快取的舊版。
- 正式網址 `hosanna0813-sys.github.io` 的前端**最慢 3 小時**自動更新;
  等不及:到 `github.com/hosanna0813-sys/hosanna0813-sys.github.io` → Actions →
  sync → Run workflow,約 1 分鐘完成。
- 驗收長音檔:看進度條是否一路推進、完成後能否切「校正版/原始版」、下載 Word/.txt。

## 6. 什麼事不用問 Claude、自己就能做

- **幫帳號加分鐘數**:Supabase SQL Editor(語法在之前對話教過;1 分鐘 = 60 credits)。
- **調整試用額度 / 上傳上限 / 並行數**:Render 環境變數
  (`TRIAL_MINUTES`、`MAX_UPLOAD_MB`、`CHUNK_WORKERS`…,完整清單見 `.env.example`)。
- **家用電腦重開機後**:什麼都不用做,Docker 會自動復活;想確認就開
  `https://你的ngrok網域/healthz` 看到 `{"ok":true}` 即正常。

## 7. 目前還沒完成的大項(要開始收錢前)

詳見 `上線檢查清單.md` B 區:
1. Render 設 `APP_ENV=production`
2. 綠界申請正式商店,填正式金鑰(`ECPAY_*`)
3. 實測一筆真實付款入帳
另外:AdSense 核准後把版位代碼填進首頁 `window.ADS`;想加第二台家用中繼照
`server/home/README.md`「加開第二台備援」做。
