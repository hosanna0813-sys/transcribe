# 上線檢查清單(語音轉逐字稿 計費版)

依「立即必做 → 收費前必做 → 安全建議 → 選填」順序。打勾即可。
網址:正式站 <https://hosanna0813-sys.github.io/>、後端 Render `transcribe-yt-j7kv.onrender.com`。

---

## A. 立即必做(讓功能完整)

### A1. Supabase 資料庫 migration(SQL Editor 各跑一次,可重複)
到 Supabase → **SQL Editor → New query**,把整份貼上按 **Run**。依序:
- [x] `v2/schema.sql`、`schema_phase2`~`phase5`、`schema_history`、`schema_pay`(早期已跑)
- [x] `v2/schema_phase6.sql`(帳務退款修正 + 試用持久化 + 約束)
- [x] `v2/schema_phase7.sql`(成本統計)
- [ ] **`v2/schema_phase8.sql`(背景任務佇列;跑之前長音檔會顯示「升級中」)** ← 目前待辦
- [ ] `v2/schema_phase9.sql`(轉錄選項:時間戳/去贅詞/講者;沒跑前選項採預設值,任務照常可建)

### A2. Supabase 登入導回網址(改根網域後必做)
- [ ] **Authentication → URL Configuration**:
  - **Redirect URLs** 加入 `https://hosanna0813-sys.github.io/v2/`(保留舊的 `.../transcribe/v2/`)
  - **Site URL** 建議也改成 `https://hosanna0813-sys.github.io/v2/`
  - 沒設的話,從新網址登入會導錯。

---

## B. 收費前必做(收真的錢之前)

### B1. 環境判斷
- [ ] Render → **Environment** 設 `APP_ENV = production`
  - 效果:金流缺正式金鑰時**直接停用儲值**(絕不用測試金鑰);`PUBLIC_BASE_URL` 變必填。

### B2. 綠界正式金鑰
- [ ] 到綠界申請個人特約商店,拿到正式 MerchantID / HashKey / HashIV
- [ ] Render 設 `ECPAY_MERCHANT_ID`、`ECPAY_HASH_KEY`、`ECPAY_HASH_IV`、`ECPAY_ENV=production`
- [ ] 確認 `PUBLIC_BASE_URL`(後端網址)、`SITE_V2_URL`(前端 v2 網址)已設
- [ ] **實測一筆真實付款**:登入 → 儲值 → 刷卡 → 幾秒後額度入帳、`payments` 出現 `paid`

### B3. OpenAI 金鑰有額度
- [ ] Render 的 `OPENAI_API_KEY` 帳戶有餘額(付費轉錄與免費試用都用它)

---

## C. 安全建議(公開前強烈建議)

### C1. 保護家用中繼(否則 ngrok 網址外流可被盜用你家頻寬)
- [ ] 產生一串長隨機密鑰(電腦執行 `openssl rand -hex 32`)
- [ ] 家用機 `server/home/.env` 填 `RELAY_SHARED_SECRET=該密鑰`,重新 `docker compose up -d --build`
- [ ] Render 設**相同**的 `RELAY_SHARED_SECRET`
- [ ] 驗收:瀏覽器直接開 `https://你的ngrok網域/info?url=...` 應得 **403**;網站 YouTube 轉錄照常

### C2. 診斷端點
- [ ] Render **不要**設 `DIAG_TOKEN`(不設 = `/diag` 一律 404,最安全)

### C2b. 多台家用中繼(備援/分流,選填)
- [ ] 第二台依 `server/home/README.md`「加開第二台備援」架設(需**另一個 ngrok 免費帳號**的網域)
- [ ] 兩台 `RELAY_SHARED_SECRET` 與 Render **完全相同**
- [ ] Render 的 `HOME_RELAY_URL` 改成兩網址**逗號分隔**;後端會自動依序嘗試、失敗換下一台
- [ ] 驗收:關掉第一台後仍能正常 YouTube 轉錄(自動改用第二台)

### C3. 隱私權政策聯絡信箱
- [ ] 把 `privacy/index.html` 末端的聯絡信箱換成你要公開的信箱(目前預設 gmail)

---

## D. AdSense(等審核 / 有版位再做)

- [x] 發布商 ID `ca-pub-5508689053185527` 已填、每頁 head 已放載入碼、可送審
- [ ] 到 <https://adsense.google.com> 用 `hosanna0813-sys.github.io` 送審(根網域已可驗證)
- [ ] 核准後:建立兩個廣告單元,把版位代碼填進 `index.html` 的 `window.ADS.slotTop`/`slotBottom`
- [ ] (提醒)ads.txt 因 GitHub 專案頁限制會顯示「earnings at risk」,不影響收入;
  日後綁自訂網域或建使用者頁 repo 可消除

---

## E. 選填調整(都有合理預設,不設也能跑)

Render 環境變數:
- [ ] `TRIAL_MINUTES`(每人每日免費試用分鐘,預設 10)
- [ ] `TRIAL_DAILY_TOTAL_MINUTES`(全站每日試用總量,預設 300)
- [ ] `FREE_DAILY_MINUTES`(登入帳號每日免費分鐘,預設關閉)
- [ ] `WORKER_CONCURRENCY`(背景任務同時數,預設 1)
- [ ] `MAX_UPLOAD_MB`(原始檔案大小上限,預設 500;檔案越大 Render 磁碟/上傳負擔越大)
- [ ] `PRICE_WHISPER_PER_MIN` / `PRICE_GPT_4O_MINI_IN_PER_1M` / `..._OUT_PER_1M`(成本統計價格)
- [ ] `CORRECTION_MODEL`(校正模型,預設 gpt-4o-mini)
- [ ] `SITE_V2_URL` 改成 `https://hosanna0813-sys.github.io/v2/`(綠界導回少一次跳轉)

完整變數清單見根目錄 `.env.example`。

---

## 目前狀態速覽
- 三頁前端已上線(首頁免費試用 / v2 計費 / byok 進階),自動從本 repo 同步到根網域。
- 安全總檢三階段已完成:中繼 HMAC、JWT 白名單、綠界回呼強化、可信 IP、健康檢查、
  帳務退款來源修正、試用持久化、成本統計、背景任務佇列 + 就地 Worker。
- **唯一擋住長音檔的待辦:跑 `schema_phase8.sql`(A1 最後一項)。**
- 收費前:完成 B 區(APP_ENV=production + 綠界正式金鑰 + 實測一筆)。
